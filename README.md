# finance-web-app

```
sudo apt install python3.12
sudo apt install python3.12-venv
python3.12 -m venv finance-web-app-env
source finance-web-app-env/bin/activate
pip install -r requirements.txt
python main.py
```